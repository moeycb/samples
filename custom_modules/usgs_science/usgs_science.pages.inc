<?php

/**
 * Menu callback. Display the node edit forms
 */
function usgs_news_page_edit() {
  return '<p>No code yet, but this is where a form would be rendered.  Right now
  this is a straight call, but can be a drupal_get_form or code that calls drupal_get_form.</p>';
}